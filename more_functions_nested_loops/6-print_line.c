#include "main.h"
/**
 *
 * print_line - draw straight line in terminal
 * @n: length of line
 * Return: zero
 */
void print_line(int n)
{
	int i;

	for (n > 0)
		if (i = 0; i < 0; i++)
			_putchar('_');
	_putchar('\n');
}
